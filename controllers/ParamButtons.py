class ParamButtons:
    def __init__(self, width_text, height_text,
                 alone_size_text, over_size_text, birth_size_text,
                 button_to_start):
        self.width_text = width_text
        self.height_text = height_text
        self.alone_size_text = alone_size_text
        self.over_size_text = over_size_text
        self.birth_size_text = birth_size_text
        self.button_to_start = button_to_start